class STAT_Instance:
    def __init__(self, game_name, instance_status, turn_count):
        self.loaded_game = game_name
        self.instance_state = instance_status
        self.turn_count = turn_count
    
    # 

    # space to store the game's default info
    
    # space to store the merged/loaded OBJECTS but sorted by category

    # space to store the numbers from a save
        # Counters, # of units.

    # space to store current effects

    # space to store current events

    # space to store actors

    # 
